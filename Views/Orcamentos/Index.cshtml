@model List<AfReparosAutomotivos.Models.Orcamentos>

@{
    ViewData["Title"] = "Lista de Orçamentos";
}

<div class=""><a asp-controller="Orcamentos" asp-action="Create" class="btn btn-primary fw-semibold">Novo Orçamento</a></div> 
<br>
<table class="table table-striped">
    <thead>
        <tr class="table-primary">
            <th class="text-left fw-semibold" scope="col">@Html.DisplayNameFor(model => model.First().idOrcamento)</th>
            <th class="text-end fw-semibold" scope="col">Ações</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model)
        {
            <tr>
                @* Apenas o valor do ID será exibido na linha *@
                <td>
                    @Html.DisplayFor(modelItem => item.idOrcamento)
                </td>
                
                @* Coluna de Ações: Ver, Editar, Excluir *@
                <td class="d-flex align-middle justify-content-end gap-2">
                    @* 1. BOTÃO VER (aponta para a Action Details) *@
                    <a asp-action="Details" asp-route-id="@item.idOrcamento" class="btn btn-sm" title="Ver">
                        <i class="fas fa-eye"></i>
                    </a>
                    
                    @* 2. BOTÃO EDITAR (aponta para a Action Edit) *@
                    <a asp-action="Edit" asp-route-id="@item.idOrcamento" class="btn btn-sm" title="Editar">
                        <i class="fas fa-pencil-alt"></i>
                    </a>
                    
                    @* 3. FORMULÁRIO EXCLUIR (aponta para a Action Delete, via POST) *@
                    <form asp-action="Delete" asp-route-id="@item.idOrcamento" method="post" style="display:inline;">
                        <button type="submit" class="btn btn-sm" title="Excluir"
                                onclick="return confirm('Tem certeza que deseja excluir o Orçamento @item.idOrcamento?');">
                            <i class="fa fa-trash-alt"></i>
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
