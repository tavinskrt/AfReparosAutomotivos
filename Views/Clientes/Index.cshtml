@model List<AfReparosAutomotivos.Models.Clientes>


@{
    ViewData["Title"] = "Lista de Clientes";
    
    var currentDocumento = Context.Request.Query["documento"].ToString();
    var currentNome = Context.Request.Query["nome"].ToString();
    var currentTelefone = Context.Request.Query["telefone"].ToString();
    var currentEndereco = Context.Request.Query["endereco"].ToString();
}

<div class="container mt-4">
    <!--div class="d-flex justify-content-between align-items-center">
        <h2 class="mb-4 text-center">Filtrar</h2>
    </div-->

    <!--form method="get" asp-action="Index" class="card shadow-sm p-4 mb-4">
        <div class="row g-3 align-items-end">

            <!-- Documento >
            <div class="col-md-4">
                <label class="form-label fw-semibold">Documento do Cliente</label>
                <div class="input-group">
                    <span class="input-group-text bg-primary text-white">
                        <i class="bi bi-person-vcard"></i>
                    </span>
                    <input type="text" name="documento" class="form-control" placeholder="Digite o CPF/CNPJ" value="@currentDocumento" />
                </div>
            </div>

            <!-- Nome >
            <div class="col-md-4">
                <label class="form-label fw-semibold">Nome do Cliente</label>
                <div class="input-group">
                    <span class="input-group-text bg-primary text-white">
                        <i class="bi bi-person-fill"></i>
                    </span>
                    <input type="text" name="nome" class="form-control" placeholder="Digite o nome" value="@currentNome" />
                </div>
            </div>

            <!-- Telefone >
            <div class="col-md-4">
                <label class="form-label fw-semibold">Telefone</label>
                <div class="input-group">
                    <span class="input-group-text bg-primary text-white">
                        <i class="bi bi-calendar-date"></i>
                    </span>
                    <input type="text" name="telefone" class="form-control" value="@currentTelefone" maxlength="11"/>
                </div>
            </div>

            <!-- Botões >
            <div class="col-md-12 d-flex gap-2">
                <button type="submit" class="btn btn-success flex-fill">
                    <i class="bi bi-search"></i> Buscar
                </button>
                <a asp-action="Index" class="btn btn-outline-secondary flex-fill">
                    <i class="bi bi-x-circle"></i> Limpar
                </a>
            </div>
        </div>
    </form-->

    @if (Model != null && Model.Any())
    {
        <h3 class="mb-3 mt-5">Clientes (@Model.Count)</h3>
        <table class="table table-striped table-hover rounded-3 overflow-hidden shadow-sm">
            <thead>
                <tr class="table-primary">
                    <th class="text-left fw-semibold" scope="col">@Html.DisplayNameFor(model => model.First().id)</th>
                    <th class="text-left fw-semibold" scope="col">Cliente</th>
                    <th class="text-left fw-semibold" scope="col">Documento</th>
                    <th class="text-left fw-semibold" scope="col">Telefone</th>
                    <th class="text-left fw-semibold" scope="col">Endereço</th>
                    <th class="text-left fw-semibold" scope="col">Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.id)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.nome)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.documento)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.telefone)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.endereco)
                        </td>
                        
                        <td class="d-flex align-middle gap-2">
                            <a asp-action="Details" asp-route-id="@item.id" class="btn btn-sm btn-info text-white" title="Ver">
                                <i class="fas fa-eye"></i>
                            </a>
                            
                            <a asp-action="Edit" asp-route-id="@item.id" class="btn btn-sm btn-warning text-dark" title="Editar">
                                <i class="fas fa-pencil-alt"></i>
                            </a>
                            
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-info text-center mt-5" role="alert">
            Nenhum cliente encontrado.
        </div>
    }
</div>